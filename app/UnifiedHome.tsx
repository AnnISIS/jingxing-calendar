"use client";
import {useState} from "react";
import {OrdinaryTemplate} from "./ordinary/OrdinaryTemplate";
import {CalendarView,augustLunar} from "./calendar/CalendarView";
import {findFestival} from "./data/festivals";
import {FestivalPoster} from "./FestivalPoster";
const CalendarIcon=()=> <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M6 3v3M18 3v3M4.5 8.5h15M5.5 5h13a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2h-13a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2Z"/></svg>;
export default function UnifiedHome(){const [mode,setMode]=useState<"today"|"calendar">("today");const [date,setDate]=useState({month:8,day:15});const festival=findFestival(date.month,date.day);const weekday=["星期日","星期一","星期二","星期三","星期四","星期五","星期六"][new Date(2026,date.month-1,date.day).getDay()];const lunar=`${date.day<=12?"六月":"七月"}${augustLunar[date.day-1]}`;const showToday=()=>{if(!festival)setDate({month:8,day:15});setMode("today")};return <div className="unified-home">{mode==="today"?(festival?<FestivalPoster festival={festival} onOpenCalendar={()=>setMode("calendar")}/>:<OrdinaryTemplate variant="amitabha" embedded day={date.day} weekday={weekday} lunarLabel={lunar} onOpenCalendar={()=>setMode("calendar")}/>):<CalendarView embedded month={date.month} day={date.day} onChange={(month,day)=>setDate({month,day})} onViewDay={()=>setMode("today")}/>}<nav className="bottom-nav unified-nav"><button className={mode==="today"?"active":""} onClick={showToday}><span className="today-dot">●</span><b>{festival?`${date.month}月${date.day}日`:"今日"}</b></button><button className={mode==="calendar"?"active":""} onClick={()=>setMode("calendar")}><CalendarIcon/><b>日历</b></button></nav></div>}
